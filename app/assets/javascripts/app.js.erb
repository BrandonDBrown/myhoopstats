var createChart = function() {
var buyerData = {
	labels : "January",
	datasets : [
		{
            label: "One",
			fillColor : "rgba(172,194,132,0.4)",
			strokeColor : "#ACC26D",
			pointColor : "#fff",
			pointStrokeColor : "#9DB86D",
			data : [<%= @user.practices.sum(:makeft) %>]
		}
//        {
//            label: "Two",
//			fillColor: "rgba(151,187,205,0.2)",
//            strokeColor: "rgba(151,187,205,1)",
//            pointColor: "rgba(151,187,205,1)",
//            pointStrokeColor: "#fff",
//			data : [[<%= Practice.all.map { |p| [p.makeft, p.totalft, p.percentageft] }.unshift(['Year', 'Sales', 'Expenses']) %>]]
//		}
    ]
}


var buyers = document.getElementById('buyers').getContext('2d');

new Chart(buyers).Line(buyerData, { scaleFontColor:"black",scaleFontSize:20, responsive: false, maintainAspectRatio: false });

    if ($(window).width() < 768 ) {
        $('#row-eq-height').removeClass('row-eq-height');
        $('#lastrow').removeClass('lastly');
        $('#lastrow').addClass('lastlymob');
    }
    else {}
    
}

    	$('#overall-link').click(function(e) {
		$("#overall-form").delay(100).fadeIn(100);
 		$("#recent-form").fadeOut(100);
		$('#recent-link').removeClass('activer');
		$(this).addClass('activeo');
		e.preventDefault();
	});
    $('#recent-link').click(function(e) {
		$("#recent-form").delay(100).fadeIn(100);
 		$("#overall-form").fadeOut(100);
		$('#overall-link').removeClass('activeo');
		$(this).addClass('activer');
		e.preventDefault();
	});
   
$(document).ready(createChart);
